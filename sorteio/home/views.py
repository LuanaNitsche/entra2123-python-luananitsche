from django.shortcuts import render

# Create your views here.
from django.shortcuts import render

def sorteio_view(request):
    # Coloque o código relacionado ao sorteio aqui
    # Pode ser o código para realizar o sorteio ou apenas renderizar um template
    return render(request, 'nome_do_template.html')  # Lembre-se de criar o template HTML
